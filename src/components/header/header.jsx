import React from "react";

const Header = () => {
    return (
        <div>
            <h2>
                This will be the header with a hamburger menu, search bar,
                phone/address area, cart.
            </h2>
        </div>
    );
};
export default Header;
