import React from "react";

const SideCart = (props) => {
    console.log("sideCart props: ", props);
    return <section className="sideCart">Test Checkout Cart</section>;
};

export default SideCart;
